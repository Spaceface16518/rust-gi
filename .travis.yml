language: rust
os:
  - linux
  - osx
  - windows
cache: cargo
rust:
  - stable
  - beta
  - nightly

# before_deploy: "cargo build --release --verbose"
# deploy:
#   skip_cleanup: true
#   provider: releases
#   api_key: $GH_TOKEN
#   file: target/release/gi
#   draft: true
#   on:
#     tags: true
